/** Max tool rows shown when a section is expanded (before "Ver todos"). */
export const SECTION_PREVIEW_MAX_ITEMS = 4

/** Max height for inline diff / tool output previews. */
export const INLINE_PREVIEW_MAX_HEIGHT_PX = 220