## 1. Trait

- [x] 1.1 Add `AdapterHealth`, `AdapterError`, `AdapterEvent`, `GenerateRequest`, and `AgentAdapter` to `circulo-adapter`
- [x] 1.2 Add unit tests for error/health shapes

## 2. Fake

- [x] 2.1 Implement `FakeAdapter` successful script and `FakeAdapter::failing()`
- [x] 2.2 Tests: probe available, successful sequence (text + tasks + tool diff + completed), failing path, no OpenCode required
